{
  "description": "install the Puppet agent (run with --tty)",
  "implementations" : [
    { "name": "install_agent.sh", "requirements": ["shell"] }
  ],
  "input_method": "environment",
  "parameters" : {
    "docker_ip": {
      "description": "the IP address for the docker host running the stack",
      "type": "String[1]"
    },
    "docker_host": {
      "description": "the hostname of the docker host",
      "type": "String[1]"
    }
  }
}
