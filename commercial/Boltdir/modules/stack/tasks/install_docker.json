{
  "description": "install Docker and Docker Compose (run with --tty)",
  "implementations" : [
    { "name": "install_docker.sh", "requirements": ["shell"] }
  ],
  "input_method": "environment",
  "parameters" : {
    "compose": {
      "description": "desired version of docker compose (default: 1.22.0)",
      "type": "Optional[String[1]]"
    }
  }
}
